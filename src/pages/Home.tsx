import React from 'react';

const Home: React.FC = () => {
    // Загрузка списка постов

    return (
        <div>
            <h2>Список постов</h2>
            {/* Вывод списка постов */}
        </div>
    );
};

export default Home;
